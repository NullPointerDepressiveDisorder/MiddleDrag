From 04c2e959c44efe2b8a88f11a229e06e667eb129d Mon Sep 17 00:00:00 2001
From: NullPointerDepressiveDisorder
 <96403086+NullPointerDepressiveDisorder@users.noreply.github.com>
Date: Wed, 7 Jan 2026 19:11:50 -0800
Subject: [PATCH 2/2] ci: Update the URL variable in the Calculate Nix hash
 step to use MiddleDrag-${VERSION}.pkg

---
 .github/workflows/update-nixpkgs.yml | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/.github/workflows/update-nixpkgs.yml b/.github/workflows/update-nixpkgs.yml
index ef36f45..b7509bd 100644
--- a/.github/workflows/update-nixpkgs.yml
+++ b/.github/workflows/update-nixpkgs.yml
@@ -42,13 +42,13 @@ jobs:
         id: hash
         run: |
           VERSION="${{ steps.version.outputs.version }}"
-          URL="https://github.com/${{ github.repository }}/releases/download/v${VERSION}/MiddleDrag.pkg"
-          
+          URL="https://github.com/${{ github.repository }}/releases/download/v${VERSION}/MiddleDrag-${VERSION}.pkg"
+
           # nix-prefetch-url returns the Nix store path and hash
           HASH=$(nix-prefetch-url --type sha256 "$URL" 2>/dev/null)
           # Convert to SRI format (sha256-base64)
           SRI_HASH=$(nix hash convert --hash-algo sha256 --to sri "$HASH")
-          
+
           echo "hash=${SRI_HASH}" >> $GITHUB_OUTPUT
           echo "SHA256 (SRI): ${SRI_HASH}"
 
@@ -73,7 +73,7 @@ jobs:
 
           # Update version
           sed -i '' "s/version = \".*\"/version = \"${VERSION}\"/" "$PACKAGE_FILE"
-          
+
           # Update hash (SRI format: sha256-XXXX=)
           sed -i '' "s|hash = \"sha256-.*\"|hash = \"${HASH}\"|" "$PACKAGE_FILE"
 
-- 
2.43.0

